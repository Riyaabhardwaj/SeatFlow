{
  "name": "seatflow",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time event booking and payment platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "dev:api": "npm run dev --workspace=api",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  }
}